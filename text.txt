  git init
  git status
  git add .
  git commit -m ""
  $ git remote add origin https://github.com/TheLegion007/intro.git
  git branch -M main
  git push u -origin main
  git log --oneliness    press :q to exit screen
  git reset --hard 7cdedb
  till day -22  33:10 done

  ➕ :heavy_plus_sign: when adding a file or implementing a feature
  🔨 :hammer: when fixing a bug or issue
  💚 :green_heart: when improving code or comments
  ⚡ :zap: when improving performance
  📜 :scroll: when updating docs or readme
  🔑 :key: when dealing with security
  🔁 :repeat: when updating dependencies or data
  ✅ :white_check_mark: when a new release was built
  👕 :shirt: when refactoring or removing linter warnings
  ❌ :x: when removing code or files
  🎨 :art: visual / UI design add

https://images.pexels.com/photos/599708/pexels-photo-599708.jpeg?cs=srgb&dl=pexels-sunil-patel-599708.jpg&fm=jpg
  bootstrap -
    - padding (pt-1 pb-2 )
    - margin (mt-2 mt-5)
    - color / bold /text
    - navbar
    - buttons / icons
    - container
    - grid (col-6 col-md-4)
    - card (d-flex justify content)
    - modal
    - forms
script--
  - Date.now() function  (a javascript function to grab a unique id which is date and time )
  -dynamic values to keep inside backtick i.e ``
  - onclick() to trigger the function -- index.html
  - document.getElementsByClassName(".className");
  or
  - document.querySelector(".className");
  - putting html template in javascript to dyanimically render it(to add new card by javascript).
  - {taskData.id} to grab that id.
  - push this child into parent i.e into taskContainer
  - local storage(Dom Storage-storing data on browser) / API-5mb(letting the data prevail even after refreshing)
  - global array and then setItem
  - stringify accepts only object , so changing array into key value pairs and then parse it.
  -
  -destructured concept of javascript i.e.  const {cards}
  - create func to store data by helping localStorage by creating array
  - globalStore - [] pushing value of card1 card2 card3...(cards are object)
  - localstorage(string) ----->Refresh------->globaldata(array of object)
  - cycle --
    -feeding in card(data) then ---globalstorage array pushes to localStorage
    - now when refereshing anything , and globalstorage is empty
    - then localStorage pushes data to globalstorage and it render it on screen.

  - onload event listener in html file
